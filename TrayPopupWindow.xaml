<Window x:Class="ClipManagerForWindows.TrayPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Width="380"
        Height="500"
        KeyDown="OnKeyDown">

    <Border CornerRadius="8"
            Background="{DynamicResource PopupBackgroundBrush}"
            BorderBrush="{DynamicResource PopupBorderBrush}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" Opacity="0.5" ShadowDepth="4" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0"
                       Text="Clipboard History"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       FontFamily="Segoe UI"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="16,14,16,10"/>

            <!-- Top Separator -->
            <Border Grid.Row="1" Height="1" Background="{DynamicResource SeparatorBrush}" Margin="0,0,0,4"/>

            <!-- Clipboard Entries List -->
            <ListBox x:Name="EntriesListBox"
                     Grid.Row="2"
                     ItemsSource="{Binding RecentEntries}"
                     ItemTemplate="{DynamicResource ClipboardEntryTemplate}"
                     ItemContainerStyle="{DynamicResource ClipboardEntryListItemStyle}"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     PreviewMouseLeftButtonUp="OnEntryClicked"
                     PreviewMouseRightButtonUp="OnEntryRightClicked">
                <ListBox.Resources>
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource TrayScrollBar}"/>
                </ListBox.Resources>
                <ListBox.ContextMenu>
                    <ContextMenu x:Name="EntryContextMenu" Style="{DynamicResource ThemedContextMenu}">
                        <MenuItem Header="Delete" Click="OnDeleteEntryClick" Style="{DynamicResource ThemedMenuItem}"/>
                    </ContextMenu>
                </ListBox.ContextMenu>
            </ListBox>

            <!-- Empty State -->
            <TextBlock x:Name="EmptyStateText"
                       Grid.Row="2"
                       Text="No clipboard entries yet"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       FontFamily="Segoe UI"
                       FontSize="13"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Visibility="Collapsed"/>

            <!-- Bottom Separator -->
            <Border Grid.Row="3" Height="1" Background="{DynamicResource SeparatorBrush}" Margin="0,4,0,4"/>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="4" Margin="0,0,0,6">
                <Button Content="Settings" Style="{DynamicResource TrayMenuButtonStyle}" Click="OnSettingsClick"/>
                <Button Content="Clear All History" Style="{DynamicResource TrayMenuButtonStyle}" Click="OnClearAllClick"/>
                <Button x:Name="DevAddRecordsButton" Content="[DEV] Add Records"
                        Style="{DynamicResource TrayMenuButtonStyle}" Click="OnDevAddRecordsClick"
                        Visibility="Collapsed"/>
                <Button Style="{DynamicResource TrayMenuButtonStyle}" Click="OnQuitClick">
                    <TextBlock Text="Quit" Foreground="{DynamicResource DangerBrush}"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
